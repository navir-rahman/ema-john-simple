import React from 'react';

const ReviewItem = (props) => {
    const {name, price, shipping, quantiry}= props.product;
    return (
        <div>
            const:{
            name
            }
        </div>
    );
};

export default ReviewItem;